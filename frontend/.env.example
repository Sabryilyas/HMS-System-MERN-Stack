# Stripe publishable key (safe to expose in the browser)
VITE_STRIPE_PUBLIC_KEY=your-stripe-publishable-key-here

# API base URL — point this at your running backend
VITE_API_URL=http://localhost:5000/api
